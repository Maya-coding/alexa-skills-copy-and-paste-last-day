///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////// THE REQUIRE GOES ON THE TOP /////////////////////////////////////////////

const websiteinfo = require("./websiteinfo.js"); // PUT YOUR NAME

/////////////////////////////////////////////////// OTHER CODE ////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

const ExampleHandler = { // PUT YOUR HANDLER NAME
  canHandle(handlerInput) {
    const request = handlerInput.requestEnvelope.request;
    return request.type === 'IntentRequest'
      && request.intent.name === 'exampleIntent'; //PUT YOUR INTENT NAME
  },
 async handle(handlerInput) {
      const speechOutput = await websiteinfo.getLastDay(); //PUT YOUR CLASS NAME
      
    return handlerInput.responseBuilder
      .speak(speechOutput)
      .getResponse();
  },
};

////////////////////////// Add your intent to the exports.handler as well. ////////////////////////

